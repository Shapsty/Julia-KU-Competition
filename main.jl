# center for gui & backend
include("frontEnd.jl")
include("weather.jl")

function main()

    # for some reason this works, and I can't change the name without breaking things, but this just calls main() from frontEnd.jl to start the application
    main()
    
end

